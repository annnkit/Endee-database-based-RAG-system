version: "3.9"

services:
  endee:
    image: endeeio/endee:latest
    container_name: endee
    ports:
      - "8080:8080"
    volumes:
      - endee_data:/data
    restart: unless-stopped

volumes:
  endee_data:
